{
  "name": "cityjobs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env dev --remote --test-scheduled",
    "deploy": "wrangler deploy --env production",
    "db:migrate": "wrangler d1 execute cityjobs-db --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.99.0"
  }
}
